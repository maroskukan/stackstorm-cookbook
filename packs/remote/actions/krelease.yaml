---
name: krelease
description: This is action is used to find the kernel release of remote server(s)
runner_type: remote-shell-cmd
entry_point: ""
enabled: true
parameters:
  cmd:
    type: string
    default: uname -r
    immutable: true